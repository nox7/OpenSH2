No payload data.

Shows a "Time until final invasion" progress bar on the screen. The progress is controlled by how many "+X months" are set in that particular ScenarioEvent's +x month setting. See [[ScenarioEvent]] for that byte data.